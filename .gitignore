/target
/private
docs/book

# macOS
.DS_Store

# SQLite runtime database files
*.sqlite
*.sqlite-shm
*.sqlite-wal
# ...except committed test fixtures
!/tests/fixtures/**/*.sqlite

# Secrets / local env (LLM API keys, Kafka SASL credentials, reset token)
.env

# Internal design/audit docs — kept local only, not for the public repo
/design-docs
/project-review

# Local runtime config (may hold private endpoints). Template: neurolithe.example.toml
/neurolithe.toml
